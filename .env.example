# Optional administration-only URL for db:migrate and db:verify (requires psql).
# The Next.js runtime does not read this value.
POSTGRES_URL=

# Supabase Data API and Auth. The publishable key is safe for browser use.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
